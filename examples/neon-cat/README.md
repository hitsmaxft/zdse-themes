# Neon Cat animation example

`neon_animation_resolve()` demonstrates a deterministic animation resolver:
the same WPM value and monotonic timestamp always produce the same character,
equalizer, and water frame indices. Asset rendering is deliberately omitted.
